#pragma once

#include "camera.h"
#include "checkerboard.h"

namespace Settings
{
	static void Read(const cv::FileNode& node, Camera, Checkerboard)
	{

	}

	void Write(cv::FileStorage& fs, Camera, Checkerboard)
	{

	}
}